/**
 * ╔═══════════════════════════════════════════════════════════════════════════╗
 * ║                         RESTful API — TZ 工业数据平台                          ║
 * ║                                                                           ║
 * ║  Base URL   : /api/v1                                                      ║
 * ║  Auth Header: Authorization: Bearer <token>                                ║
 * ║  Content-Type: application/json                                            ║
 * ║  Encoding   : UTF-8                                                       ║
 * ║                                                                           ║
 * ║  全局状态码                                                                  ║
 * ║  ─────────────────────────────────────────────                            ║
 * ║  200  OK                     请求成功                                      ║
 * ║  201  Created                资源创建成功                                  ║
 * ║  204  No Content             删除成功（无返回体）                             ║
 * ║  400  Bad Request            请求参数格式错误或字段校验失败                    ║
 * ║  401  Unauthorized           未认证或 token 过期                            ║
 * ║  403  Forbidden              无权限访问该资源                               ║
 * ║  404  Not Found              资源不存在                                    ║
 * ║  409  Conflict               数据并发冲突（如乐观锁版本不匹配）                  ║
 * ║  422  Unprocessable Entity   业务规则校验失败                                ║
 * ║  500  Internal Server Error 服务器内部错误                                  ║
 * ╚═══════════════════════════════════════════════════════════════════════════╝
 *
 *
 * ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
 * ┃  View 1 — 锅炉/气化当日消耗台账  (DailyConsumption.vue)                      ┃
 * ┃  功能概述：固定 4 行表格（第一次/第二次/第三次/总计），两表：锅炉 + 气化            ┃
 * ┃  存储：日报，设备维度（207A/207B），无主键自增，每行为批次维度固定记录              ┃
 * ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  GET  /daily-consumption
 * ─────────────────────────────────────────────────────────────────────────────
 * 获取当日消耗台账数据。
 *
 * Query Parameters:
 *   date  string   可选。格式 YYYY-MM-DD。缺省返回当天记录。
 *
 * Response 200:
 * {
 *   "code": 200,
 *   "data": {
 *     "boiler": {
 *       "rows": [
 *         { "batch": "第一次", "subtotal": "0", "hl_A": "0", "hl_B": "0",
 *           "jz_A": "0", "jz_B": "0", "xz_A": "0", "xz_B": "0",
 *           "wn_A": "0", "wn_B": "0", "yl_A": "0", "yl_B": "0",
 *           "lx_A": "0", "lx_B": "0" },
 *         { "batch": "第二次", ... },
 *         { "batch": "第三次", ... },
 *         { "batch": "总计", "subtotal": "0", ... }   ← 后端自动计算，前端仅展示
 *       ]
 *     },
 *     "gasification": {
 *       "rows": [
 *         { "batch": "第一次", "subtotal": "0", "coal_A": "0", "coal_B": "0" },
 *         { "batch": "第二次", ... },
 *         { "batch": "第三次", ... },
 *         { "batch": "总计", ... }   ← 后端自动计算
 *       ]
 *     },
 *     "date": "2024-03-12",
 *     "savedAt": 1710220800000    // 上次保存时间戳，毫秒
 *   }
 * }
 *
 * Error Responses:
 *   404 — 当日无数据，返回空 rows 数组而非 404
 *   422 — date 格式非法
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  POST  /daily-consumption
 * ─────────────────────────────────────────────────────────────────────────────
 * 全量保存当日消耗台账。全部字段平铺，无嵌套 JSON。
 *
 * Request Body:
 * {
 *   "boiler": {
 *     "rows": [
 *       {
 *         "batch":    "第一次" | "第二次" | "第三次" | "总计",
 *         "subtotal": string,   // "0" ~ "999999"，正整数或小数（最多3位小数）
 *         "hl_A":     string,   // 黄陵混合煤 207A 消耗量
 *         "hl_B":     string,   // 黄陵混合煤 207B 消耗量
 *         "jz_A":     string,   // 建庄大块煤 207A 消耗量
 *         "jz_B":     string,   // 建庄大块煤 207B 消耗量
 *         "xz_A":     string,   // 细渣煤 207A 消耗量
 *         "xz_B":     string,   // 细渣煤 207B 消耗量
 *         "wn_A":     string,   // 污泥 207A 消耗量 ← 独立核算，不计入燃料煤 207 限额
 *         "wn_B":     string,   // 污泥 207B 消耗量 ← 独立核算，不计入燃料煤 207 限额
 *         "yl_A":     string,   // 原料煤 207A 消耗量
 *         "yl_B":     string,   // 原料煤 207B 消耗量
 *         "lx_A":     string,   // 离心煤 207A 消耗量
 *         "lx_B":     string    // 离心煤 207B 消耗量
 *       }
 *     ]
 *   },
 *   "gasification": {
 *     "rows": [
 *       {
 *         "batch":    "第一次" | "第二次" | "第三次" | "总计",
 *         "subtotal": string,
 *         "coal_A":   string,   // A仓原料煤消耗量
 *         "coal_B":   string    // B仓原料煤消耗量
 *       }
 *     ]
 *   },
 *   "savedAt": 1710220800000   // 毫秒时间戳，客户端提交时自动填充
 * }
 *
 * Validation Rules:
 *   • batch 必须是 ["第一次","第二次","第三次","总计"] 之一，且行数固定为 4
 *   • subtotal / 所有 silo 字段：非空，正数，最多 3 位小数
 *   • wn_A / wn_B：须单独记录，不得与其他燃料煤共享限额
 *   • subtotal 不参与计算，仅展示；总计行 subtotal 由后端根据 sum(其他列) 计算得出
 *
 * Response 200:
 * { "code": 200, "message": "保存成功", "savedAt": 1710220800000 }
 *
 * Error Responses:
 *   400 — JSON 解析失败或缺少必需字段
 *   422 — 字段值校验失败（如 batch 不合法、数值格式错误）
 *   409 — 同一天数据已被他人覆盖（乐观锁），返回最新 savedAt
 *
 *
 *
 *
 * ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
 * ┃  View 2 — 干化污泥卸输煤台账  (DriedSludge.vue)                            ┃
 * ┃  功能概述：每日动态增删改查，按月分组分页，所有操作针对 dried_sludge_rows 表      ┃
 * ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  GET  /dried-sludge
 * ─────────────────────────────────────────────────────────────────────────────
 * 获取干化污泥台账全部记录，按 date 升序返回。
 *
 * Query Parameters:
 *   month  string   可选。格式 "YYYY-MM"。缺省返回所有月份。
 *
 * Response 200:
 * {
 *   "code": 200,
 *   "data": [
 *     {
 *       "id":          "uuid-v4",
 *       "date":        "2024-03-12",
 *       "bin":         "207A",           // 掺烧煤仓
 *       "boilerDay":   "0",              // 锅炉当日掺烧干化污泥（吨）
 *       "boilerMonth": "0",              // 锅炉当月掺烧干化污泥（吨）
 *       "boilerYear":  "0",              // 锅炉当年掺烧干化污泥（吨）
 *       "inbound":     "0",              // 干化污泥入仓量（吨）
 *       "stockA":      "0",              // 207A库存量（吨）
 *       "stockB":      "0",              // 207B库存量（吨）
 *       "stockTotal":  "0",             // 总库存量 = stockA + stockB（吨）
 *       "createdAt":   1710220800000,
 *       "updatedAt":   1710220800000
 *     }
 *   ]
 * }
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  GET  /dried-sludge/:id
 * ─────────────────────────────────────────────────────────────────────────────
 * 根据主键 ID 获取单条记录。
 *
 * Path Parameters:
 *   id  string   记录的 UUID v4
 *
 * Response 200: { "code": 200, "data": { ... row object ... } }
 * Response 404: { "code": 404, "message": "记录不存在" }
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  POST  /dried-sludge
 * ─────────────────────────────────────────────────────────────────────────────
 * 新增一条干化污泥记录。id 由后端生成 UUID 并返回。
 *
 * Request Body:
 * {
 *   "date":        "2024-03-12",   // 必填，格式 YYYY-MM-DD
 *   "bin":         "207A",         // 必填，掺烧煤仓
 *   "boilerDay":   "12.5",        // 可选，锅炉当日掺烧干化污泥（吨）
 *   "boilerMonth": "350",         // 可选，锅炉当月掺烧干化污泥（吨）
 *   "boilerYear":  "4200",        // 可选，锅炉当年掺烧干化污泥（吨）
 *   "inbound":     "0",            // 可选，干化污泥入仓量（吨）
 *   "stockA":      "0",            // 可选，207A库存量（吨）
 *   "stockB":      "0",            // 可选，207B库存量（吨）
 *   "stockTotal":  "0"             // 可选，若不传则后端自动计算 stockA + stockB
 * }
 *
 * Validation Rules:
 *   • date 必填，格式 YYYY-MM-DD，不可与已有记录重复
 *   • bin 必填，枚举值 ["207A", "207B", "混合"]
 *   • 所有数字字段：非负数，最多 3 位小数；空字符串视为 0
 *   • stockTotal 若不传，后端自动计算 stockA + stockB 并写入
 *
 * Response 201:
 * { "code": 201, "data": { ...完整记录含 id ... }, "message": "创建成功" }
 *
 * Error Responses:
 *   400 — JSON 解析失败或缺少必填字段
 *   422 — date 格式错误 / date 重复 / bin 不在枚举中
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  PUT  /dried-sludge/:id
 * ─────────────────────────────────────────────────────────────────────────────
 * 全量更新指定记录（替换式更新，缺失字段将写入空值）。
 * 若需部分更新请使用 PATCH。
 *
 * Path Parameters:
 *   id  string   记录 UUID
 *
 * Request Body: 同 POST，仅 id 不可修改。
 *
 * Response 200: { "code": 200, "data": { ...更新后完整记录... }, "message": "更新成功" }
 *
 * Error Responses:
 *   400 — 缺少必填字段
 *   404 — id 不存在
 *   422 — 字段校验失败
 *   409 — 乐观锁版本不匹配（若启用版本号）
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  PATCH  /dried-sludge/:id
 * ─────────────────────────────────────────────────────────────────────────────
 * 部分更新指定记录，仅更新传入的非空字段。
 *
 * Path Parameters:
 *   id  string   记录 UUID
 *
 * Request Body: 任意子集字段，null 值跳过更新。
 *   例: { "boilerDay": "15", "inbound": "20" }
 *
 * Response 200: { "code": 200, "data": { ...更新后完整记录... } }
 * Response 404: { "code": 404, "message": "记录不存在" }
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  DELETE  /dried-sludge/:id
 * ─────────────────────────────────────────────────────────────────────────────
 * 删除指定记录。
 *
 * Path Parameters:
 *   id  string   记录 UUID
 *
 * Response 204: No Content
 * Response 404: { "code": 404, "message": "记录不存在" }
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  DELETE  /dried-sludge
 * ─────────────────────────────────────────────────────────────────────────────
 * 清空整张表（需二次确认）。
 *
 * Query Parameters:
 *   confirm  string   必填，固定值 "confirm"
 *
 * Response 204: No Content
 * Response 400: { "code": 400, "message": "缺少 confirm 参数或值不正确" }
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  POST  /dried-sludge/bulk
 * ─────────────────────────────────────────────────────────────────────────────
 * 全量覆盖写入（替换式，用于整表保存）。
 * 行为：先清空 dried_sludge_rows 表，再批量插入新数据。
 *
 * Request Body:
 * {
 *   "rows": [
 *     { "date": "2024-03-12", "bin": "207A", "boilerDay": "0",
 *       "boilerMonth": "0", "boilerYear": "0", "inbound": "0",
 *       "stockA": "0", "stockB": "0", "stockTotal": "0" },
 *     ...
 *   ]
 * }
 *
 * Response 200:
 * { "code": 200, "data": { "count": 45 }, "message": "批量保存成功，共 45 条" }
 *
 * Error Responses:
 *   400 — rows 格式错误
 *   422 — rows 中存在校验失败的记录（返回失败行索引和原因）
 *   409 — 并发冲突，提示重新加载
 *
 *
 *
 *
 * ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
 * ┃  View 3 — 细渣提精煤卸输煤台账  (FineSlagCoal.vue)                        ┃
 * ┃  功能概述：每日动态增删改查，按月分组分页，所有操作针对 fine_slag_coal_rows 表   ┃
 * ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  GET  /fine-slag-coal
 * ─────────────────────────────────────────────────────────────────────────────
 * 获取细渣提精煤台账全部记录，按 date 升序返回。
 *
 * Query Parameters:
 *   month  string   可选。格式 "YYYY-MM"。
 *
 * Response 200:
 * {
 *   "code": 200,
 *   "data": [
 *     {
 *       "id":          "uuid-v4",
 *       "date":        "2024-03-12",
 *       "bin":         "207A",           // 掺烧煤仓
 *       "boilerDay":   "0",              // 锅炉当日掺烧细渣量（吨）
 *       "boilerMonth": "0",              // 锅炉当月掺烧细渣量（吨）
 *       "boilerYear":  "0",              // 锅炉当年掺烧细渣量（吨）
 *       "inbound":     "0",              // 细渣提精煤来煤量（吨）
 *       "stockA":      "0",              // 207A存煤量（吨）
 *       "stockB":      "0",              // 207B存煤量（吨）
 *       "stockTotal":  "0",             // 总存煤量 = stockA + stockB（吨）
 *       "createdAt":   1710220800000,
 *       "updatedAt":   1710220800000
 *     }
 *   ]
 * }
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  GET  /fine-slag-coal/:id
 * ─────────────────────────────────────────────────────────────────────────────
 * Response 200: { "code": 200, "data": { ... } }
 * Response 404: { "code": 404, "message": "记录不存在" }
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  POST  /fine-slag-coal
 * ─────────────────────────────────────────────────────────────────────────────
 * 新增一条细渣提精煤记录。
 *
 * Request Body:
 * {
 *   "date":        "2024-03-12",   // 必填
 *   "bin":         "207A",         // 必填，枚举 ["207A","207B","混合"]
 *   "boilerDay":   "0",            // 锅炉当日掺烧细渣量（吨）
 *   "boilerMonth": "0",            // 锅炉当月掺烧细渣量（吨）
 *   "boilerYear":  "0",            // 锅炉当年掺烧细渣量（吨）
 *   "inbound":     "0",            // 细渣提精煤来煤量（吨）
 *   "stockA":      "0",
 *   "stockB":      "0",
 *   "stockTotal":  "0"             // 可选，后端自动计算
 * }
 *
 * Validation Rules:
 *   • date 必填，YYYY-MM-DD，不可重复
 *   • bin 必填，枚举 ["207A","207B","混合"]
 *   • 所有数字字段：非负，最多 3 位小数
 *
 * Response 201: { "code": 201, "data": { ...含 id... }, "message": "创建成功" }
 * Response 422: { "code": 422, "message": "date 已存在" } 或字段校验错误
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  PUT  /fine-slag-coal/:id
 * ─────────────────────────────────────────────────────────────────────────────
 * 全量更新。Response 200 / 404 / 422 / 409 同 dried-sludge。
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  PATCH  /fine-slag-coal/:id
 * ─────────────────────────────────────────────────────────────────────────────
 * 部分更新。Response 200 / 404。
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  DELETE  /fine-slag-coal/:id
 * ─────────────────────────────────────────────────────────────────────────────
 * Response 204 / 404。
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  DELETE  /fine-slag-coal
 * ─────────────────────────────────────────────────────────────────────────────
 * 清空整表，需 confirm=confirm 参数。Response 204 / 400。
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  POST  /fine-slag-coal/bulk
 * ─────────────────────────────────────────────────────────────────────────────
 * 全量覆盖写入（先清空后批量插入）。
 *
 * Request Body:
 * {
 *   "rows": [
 *     { "date": "2024-03-12", "bin": "207A", "boilerDay": "0",
 *       "boilerMonth": "0", "boilerYear": "0", "inbound": "0",
 *       "stockA": "0", "stockB": "0", "stockTotal": "0" }
 *   ]
 * }
 *
 * Response 200:
 * { "code": 200, "data": { "count": 45 }, "message": "批量保存成功，共 45 条" }
 *
 *
 *
 *
 * ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
 * ┃  View 4 — 原料煤卸输煤台账  (RawCoal.vue)                                 ┃
 * ┃  功能概述：每日动态增删改查，按月分组分页，所有操作针对 raw_coal_rows 表        ┃
 * ┃  与 FineSlagCoal.vue 字段相同，额外包含 blendBurn（气化掺烧原料煤量）          ┃
 * ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  GET  /raw-coal
 * ─────────────────────────────────────────────────────────────────────────────
 * 获取原料煤台账全部记录。
 *
 * Query Parameters:
 *   month  string   可选，格式 "YYYY-MM"
 *
 * Response 200:
 * {
 *   "code": 200,
 *   "data": [
 *     {
 *       "id":          "uuid-v4",
 *       "date":        "2024-03-12",
 *       "bin":         "207A",           // 运行煤仓
 *       "boilerDay":   "0",              // 气化当日上煤量（吨）
 *       "boilerMonth": "0",              // 气化当月上煤量（吨）
 *       "boilerYear":  "0",              // 气化当年上煤量（吨）
 *       "inbound":     "0",              // 原料煤来煤量（吨）
 *       "stockA":      "0",              // 207A存煤量（吨）
 *       "stockB":      "0",              // 207B存煤量（吨）
 *       "stockTotal":  "0",             // 原料煤总存煤量 = stockA + stockB
 *       "blendBurn":   "0",             // 气化掺烧原料煤量（吨）← 独有字段
 *       "createdAt":   1710220800000,
 *       "updatedAt":   1710220800000
 *     }
 *   ]
 * }
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  GET  /raw-coal/:id
 * ─────────────────────────────────────────────────────────────────────────────
 * Response 200: { "code": 200, "data": { ... } }
 * Response 404: { "code": 404, "message": "记录不存在" }
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  POST  /raw-coal
 * ─────────────────────────────────────────────────────────────────────────────
 * 新增一条原料煤记录。
 *
 * Request Body:
 * {
 *   "date":        "2024-03-12",   // 必填
 *   "bin":         "207A",         // 必填，枚举 ["207A","207B","混合"]
 *   "boilerDay":   "0",            // 气化当日上煤量（吨）
 *   "boilerMonth": "0",            // 气化当月上煤量（吨）
 *   "boilerYear":  "0",            // 气化当年上煤量（吨）
 *   "inbound":     "0",            // 原料煤来煤量（吨）
 *   "stockA":      "0",
 *   "stockB":      "0",
 *   "stockTotal":  "0",           // 可选，后端自动计算
 *   "blendBurn":   "0"            // 气化掺烧原料煤量（吨）← 独有字段
 * }
 *
 * Validation Rules:
 *   • date 必填，YYYY-MM-DD，不可重复
 *   • bin 必填，枚举 ["207A","207B","混合"]
 *   • blendBurn：非负，最多 3 位小数
 *
 * Response 201: { "code": 201, "data": { ...含 id... }, "message": "创建成功" }
 * Response 422: { "code": 422, "message": "date 已存在" } 等校验错误
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  PUT  /raw-coal/:id
 * ─────────────────────────────────────────────────────────────────────────────
 * 全量更新。Response 200 / 404 / 422 / 409。
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  PATCH  /raw-coal/:id
 * ─────────────────────────────────────────────────────────────────────────────
 * 部分更新。Response 200 / 404。
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  DELETE  /raw-coal/:id
 * ─────────────────────────────────────────────────────────────────────────────
 * Response 204 / 404。
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  DELETE  /raw-coal
 * ─────────────────────────────────────────────────────────────────────────────
 * 清空整表，需 confirm=confirm 参数。Response 204 / 400。
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  POST  /raw-coal/bulk
 * ─────────────────────────────────────────────────────────────────────────────
 * 全量覆盖写入（先清空后批量插入）。
 *
 * Request Body:
 * {
 *   "rows": [
 *     { "date": "2024-03-12", "bin": "207A", "boilerDay": "0",
 *       "boilerMonth": "0", "boilerYear": "0", "inbound": "0",
 *       "stockA": "0", "stockB": "0", "stockTotal": "0",
 *       "blendBurn": "0" }
 *   ]
 * }
 *
 * Response 200:
 * { "code": 200, "data": { "count": 45 }, "message": "批量保存成功，共 45 条" }
 *
 *
 *
 *
 * ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
 * ┃  View 5 — 原料煤卸输煤台账  (FuelCoal.vue)  ⚠ 修复提醒                       ┃
 * ┃  功能概述：原料煤台账，API Store 名称目前错误指向 dried_sludge_rows              ┃
 * ┃  应改为 raw_coal_rows 或新建 fuel_coal_rows                                  ┃
 * ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
 *
 * ⚠ 当前问题：
 *   FuelCoal.vue 中的 initData() / persist() / dbUpsertRow() / dbDeleteRow()
 *   / handleClearAll() 全部使用了 'dried_sludge_rows' 这个 API Store。
 *   这会导致 FuelCoal 的数据写入到 DriedSludge 的表里，产生数据混淆。
 *
 *   建议处理方式（二选一）：
 *     A. 将 FuelCoal.vue 中所有 'dried_sludge_rows' 改为 'fuel_coal_rows'，
 *        并在后端创建对应的 fuel_coal_rows 表和 API 端点。
 *     B. 确认 FuelCoal 与 RawCoal 为同一实体，若是则将 FuelCoal.vue 废弃，
 *        统一使用 RawCoal.vue。
 *
 * ─────────────────────────────────────────────────────────────────────────────
 *  修复后端端点（方案 A）:
 * ─────────────────────────────────────────────────────────────────────────────
 *
 *  GET  /fuel-coal
 *  GET  /fuel-coal/:id
 *  POST  /fuel-coal
 *  PUT  /fuel-coal/:id
 *  PATCH  /fuel-coal/:id
 *  DELETE  /fuel-coal/:id
 *  DELETE  /fuel-coal
 *  POST  /fuel-coal/bulk
 *
 *   字段定义与 RawCoal.vue 完全一致（包含 blendBurn）。
 *   参见上方 RawCoal.vue API 注释。
 *
 *
 *
 *
 * ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
 * ┃  全局共享字段说明                                                          ┃
 * ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
 *
 * ┌─────────────────┬────────────────────────────────────────────────────────┐
 * │ 字段            │ 说明                                                   │
 * ├─────────────────┼────────────────────────────────────────────────────────┤
 * │ id              │ UUID v4，主键，PostgreSQL / SQLite 自动生成             │
 * │ date            │ 日期，格式 YYYY-MM-DD，唯一约束                         │
 * │ bin             │ 煤仓编号，枚举: 207A / 207B / 混合                       │
 * │ boilerDay       │ 当日上煤/掺烧量（吨），非负数，最多 3 位小数              │
 * │ boilerMonth     │ 当月上煤/掺烧量（吨），非负数，最多 3 位小数              │
 * │ boilerYear      │ 当年上煤/掺烧量（吨），非负数，最多 3 位小数              │
 * │ inbound         │ 来煤量（吨），非负数，最多 3 位小数                     │
 * │ stockA          │ 207A 存煤量（吨），非负数，最多 3 位小数                 │
 * │ stockB          │ 207B 存煤量（吨），非负数，最多 3 位小数                 │
 * │ stockTotal      │ 总存煤量 = stockA + stockB，后端自动计算                 │
 * │ blendBurn       │ 掺烧量（吨），仅 RawCoal / FuelCoal 有此字段             │
 * │ wn_A / wn_B    │ 污泥 207A/207B 消耗量，独立核算，不计入燃料煤 207 限额     │
 * │ createdAt       │ 毫秒时间戳，记录创建时间                                 │
 * │ updatedAt       │ 毫秒时间戳，每次更新自动刷新                             │
 * └─────────────────┴────────────────────────────────────────────────────────┘
 *
 *  数字字段空字符串处理：
 *    前端所有数字字段默认值为 "0"；后端接收时：
 *      - 空字符串 "" → 视为 0
 *      - "12.5"     → 12.5
 *      - "abc"      → 422 校验错误
 *      - null       → 视为 0
 *
 *  业务规则：
 *    1. stockTotal = stockA + stockB，后端自动维护，前端传值可忽略
 *    2. wn_A / wn_B（污泥）必须独立记录，不得与其他燃料煤共享 207 库存限额
 *    3. date 字段唯一，每日每煤仓至多一条记录
 *    4. boilerDay / boilerMonth / boilerYear 为累计值，非日增量
 *
 *
 *
 * ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
 * ┃  错误响应统一格式                                                          ┃
 * ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
 *
 * {
 *   "code":    400 | 401 | 403 | 404 | 409 | 422 | 500,
 *   "message": "中文错误描述",
 *   "errors": [              // 仅 422 时有 errors 数组
 *     { "field": "date", "message": "日期格式错误，应为 YYYY-MM-DD" },
 *     { "field": "bin",   "message": "bin 必须是 207A、207B 或 混合" }
 *   ],
 *   "data": null
 * }
 */
